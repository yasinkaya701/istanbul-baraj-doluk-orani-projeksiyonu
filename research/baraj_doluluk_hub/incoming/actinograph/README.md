# Actinograph Landing Zone

Put raw actinograph files here exactly as received.

Rules:

- Do not rename raw files unless there is a duplicate collision.
- Do not edit raw files in place.
- Keep vendor exports, screenshots, calibration notes, and metadata together.
- After landing files here, register the batch in:
  `registry/datasets/local_data_inventory.csv`
- Parsed outputs should go to:
  `/Users/yasinkaya/Hackhaton/output/actinograph/`
